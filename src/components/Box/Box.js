import { color, space, layout, shadow, border, flexbox } from 'styled-system';
import styled from 'styled-components';

const Box = styled('div')(color, space, layout, shadow, border, flexbox);

export default Box;
