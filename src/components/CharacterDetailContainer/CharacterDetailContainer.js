import { Container } from './CharacterDetailContainer.styled';

const CharacterDetailContainer = ({ children }) => {
  return <Container>{children}</Container>;
};

export default CharacterDetailContainer;
